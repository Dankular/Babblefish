# BabbleFish CPU Server Requirements
# Optimized for multi-core CPU processing (128+ threads)

# Core web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
websockets>=12.0

# SeamlessM4T v2 (all-in-one ASR + Translation)
transformers>=4.35.0
sentencepiece>=0.1.99
torch>=2.0.0
torchaudio>=2.0.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.11.0

# TTS (Kokoro with ONNX Runtime)
kokoro-onnx>=0.1.0
onnxruntime>=1.16.0  # CPU optimized

# Speaker diarization
pyannote.audio>=3.1.0

# Utilities
numpy>=1.24.0
psutil>=5.9.0  # For monitoring CPU usage

# Note: Hugging Face authentication required for pyannote models
# Run: huggingface-cli login
# Accept terms at: https://huggingface.co/pyannote/embedding
